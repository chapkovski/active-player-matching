Matching only active players
=============================

This app matches only those players who are active at the wait page:

they need to move a mouse or touch a keyboard at least once in the last `n` seconds.

